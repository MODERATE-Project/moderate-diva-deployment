# Keycloak Variables
---

files_to_fill: 
  - docker-compose.yml

keycloak_cred:
    keyck_user: "${KEYCLOAK_USER}"
    keyck_psw: "${KEYCLOAK_PASSWORD}"
    keyck_db_user: "${KEYCLOAK_DB_USER}"
    keyck_db_psw: "${KEYCLOAK_DB_PASSWORD}" 